# Use a lightweight web server for serving static files
FROM nginx:alpine

# Copy the frontend code to the nginx html directory
COPY index.html /usr/share/nginx/html/

# Expose the port that the web server listens on
EXPOSE 80

