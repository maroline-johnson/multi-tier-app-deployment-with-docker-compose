# Use a lightweight PHP image with Apache pre-installed
FROM php:7.4-apache

# Set the working directory for the application
WORKDIR /var/www/html

# Copy the PHP code to the container
COPY db.php .

# Expose the port that Apache listens on
EXPOSE 80

